// import { ipcMain } from 'electron'

export const handleLoginOperation = (): void => {
  // ipcMain.handle('resize-window', (event, args) => {
  //   // Handle the resize-window event here
  // })
}
